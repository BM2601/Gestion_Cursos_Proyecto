<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestioncursos">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestionCursos">

        <activity android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Alumno -->
        <activity android:name=".activities.alumno.AlumnoDashboardActivity"/>
        <activity android:name=".activities.alumno.BuscarCursosActivity"/>
        <activity android:name=".activities.alumno.DetalleCursoActivity"/>

        <!-- Profesor -->
        <activity android:name=".activities.profesor.ProfesorDashboardActivity"/>
        <activity android:name=".activities.profesor.CrearEditarCursoActivity"/>
        <activity android:name=".activities.profesor.AlumnosInscritosActivity"/>

        <!-- Admin -->
        <activity android:name=".activities.admin.AdminDashboardActivity"/>
        <activity android:name=".activities.admin.GestionCursosAdminActivity"/>

    </application>
</manifest>